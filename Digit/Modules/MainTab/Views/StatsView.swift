import SwiftUI

struct StatsView: View {
    var body: some View {
        VStack {
            Text("Stats")
                .font(.title)
                .foregroundColor(.brandBlue)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    StatsView()
} 